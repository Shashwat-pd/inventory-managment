

interface PageProps {
  params: Promise<{ slug: string }>; // params is now a Promise
}

import React from 'react'

const Departments = () => {
  return (
    <div>
      
    </div>
  )
}

export default Departments
